
class User:
    def __init__(self, name, password):
        self.name = name
        self.password = password

    def login():
        print(f"{self.name} ha iniciado sesión.")

    def logout():
        print(f"{self.name} ha cerrado sesión.")